export enum PageMode {
    ADD = "add",
    EDIT = "edit",
    DELETE = "delete",
    VIEW = "view",
}

export enum EtlProcessConfigStatus {
    ENABLED = "enabled",
    DISABLED = "disabled",
}

export enum EtlProcessStatus {
    SCHEDULED = "scheduled",
    RUNNING = "running",
    COMPLETED = "completed",
    FAILED = "failed",    
}